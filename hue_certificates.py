# hue certificates

ip = "xxx.xxx.x.x"
username = "XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"

# light to change
office_light = "light_name"

# to communicate with Hue lights
# hue = requests.get("https://<ip>/debug/clip.html")
# hue = requests.get("http://<ip>/api/<username>/lights/<office_light>")
# hue_office_on = requests.put("http://<ip>/api/<username>/lights/<office_light>/state", '{"on":true}')
# hue_office_off = requests.put("http://<ip>/api/<username>/lights/<office_light>/state", '{"on":false}')
